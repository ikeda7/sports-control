import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // Testes de integração serão adicionados futuramente.
    expect(true, isTrue);
  });
}
