# Normaliza line endings para LF no repositório (padrão cross-platform)
* text=auto eol=lf

# Binários — nunca converter
*.png binary
*.jpg binary
*.ico binary
*.jar binary

# Artefatos gerados — excluídos das estatísticas de linguagem do GitHub.
# drift_worker.dart.js é Dart compilado pelo Drift, não JavaScript escrito à mão.
web/drift_worker.dart.js linguist-generated=true
